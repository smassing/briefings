# briefings
daily and weekly briefings

## Adding an edition

1. Add the dated HTML file under `daily/` or `weekly/` using the `YYYY-MM-DD.html` naming format.
2. Add its relative path to `briefings-index.js`.

The archive shell in `index.html` sorts indexed editions newest-first and builds the sidebar automatically.
